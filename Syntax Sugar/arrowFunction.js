// using arrow function syntax to return the double of the number
const double = (number) => 2 * number;
console.log(`2 * 8 is ${double(8)}`);

// using arrow function syntax in array helper
const numbers = [1, 2, 3];
console.log(numbers.map(number => 2* number));