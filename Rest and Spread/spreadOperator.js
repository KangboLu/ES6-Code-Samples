const defaultColors = ['red', 'green'];
const userFavoriteColors = ['orange', 'yellow'];
// Spreads an array into its individual values.
const colorList = [...defaultColors, ...userFavoriteColors];

console.log(colorList);